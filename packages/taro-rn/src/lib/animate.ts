import { Animated, Easing } from "react-native";

export const animate = (selector, keyframes, duration, callback) => {
  console.log('hello animate')
  // console.log(selector['_internalFiberInstanceHandleDEV'].memoizedProps.style)
  // console.log(keyframes)
  // keyframes.forEach(item => {
  //   selector['_internalFiberInstanceHandleDEV'].memoizedProps.style = item
  // })
  // console.log(selector)
  // return {}
}
