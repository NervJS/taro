<div class="progress">
  <div
    if="{{showInfo && type == 'horizontal'}}"
    class="progress-horizontal-showInfo"
    style="height: 100px;"
  >
    <progress
      style="color: {{backgroundColor}};background-color:{{color}};stroke-width:{{strokeWidth}}"
      class="{{cls}}"
      type="{{type}}"
      percent="{{percent}}"
      secondarypercent="{{secondarypercent}}"
      clockwise="{{clockwise}}"
    >
    </progress>
    <text
      style="font-size: 25px;width: 80px; text-align: center;"
    >{{percent}}%</text>
  </div>
  <progress
    else
    style="color: {{backgroundColor}};background-color:{{color}};stroke-width:{{strokeWidth}}"
    class="{{cls}}"
    type="{{type}}"
    percent="{{percent}}"
    secondarypercent="{{secondarypercent}}"
    clockwise="{{clockwise}}"
  >
  </progress>
</div>
