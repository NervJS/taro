<div>
  <input
    id="{{id}}"
    value="{{ value }}"
    class="{{ cls }}"
    type="radio"
    name="{{ groupId }}"
    disabled="{{disabled}}"
    checked="{{ checked }}"
    style="color: {{color}};"
    @change="onChange"
    >
  </input>
</div>

