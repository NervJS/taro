import { getStore, setStore } from './store'
import { observer } from './observer'
import { inject } from './inject'

class Provider {}

export default {
  getStore,
  setStore,
  observer,
  inject,
  Provider
}

export {
  getStore,
  setStore,
  observer,
  inject,
  Provider
}
