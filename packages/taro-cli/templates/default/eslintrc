{
  "extends": ["taro"],
  "rules": {
    "no-unused-vars": ["error", { "varsIgnorePattern": "Taro" }]
  },
  "parser": "babel-eslint",
  globals: {
    wx: true,
    getApp: true,
    getCurrentPages: true
   }
}
